# @beforewave/agent-helm-ui-contract

Browser-safe shared Agent Helm UI contract for locale dictionaries, capability presentation metadata, summary projection, and aggregate connection/attention rules.
